import cn from 'classnames';
import { CSSProperties, ComponentChildren } from 'preact';
import './index.css';

export interface <%= componentName %>Props {
  children?: ComponentChildren;
  className?: string;
  style?: CSSProperties;
}

export function <%= componentName %>({ children, className, style }: <%= componentName %>Props) {
  return <<%= componentName %> className={cn('<%= basename %>', className)} style={style}>{children}</<%= componentName %>>;
}
